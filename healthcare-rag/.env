# Optional: OpenAI API Key for generating responses
# If left commented out or empty, the assistant will use a local mock for generation to prevent errors.
# OPENAI_API_KEY="sk-..."

# If you want to use local embeddings, no key is needed. The system defaults to HuggingFace 'all-MiniLM-L6-v2'.
